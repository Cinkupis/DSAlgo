package com.exercises.leetcode.arrays.medium;

/*
Given an array of values, design and code an algorithm that returns
1. whether there are two duplicates within k indices of each other?
2. k indices and within plus or minus l (value) of each other?

Do all, even the latter, in O(n) running time and O(k) space.
 */

@SuppressWarnings("unused")
public class PalantirDuplicatesWithingKIndices {
    public boolean duplicatesWithinK(int[] nums, int k) {

        return false;
    }
}
